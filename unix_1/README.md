# UNIX 1

This directory contains exercises and files related to **UNIX/Linux command-line operations** and basic terminal usage. The focus is on familiarizing with core commands, file system navigation, and script execution within a UNIX-like environment.

## Contents:
- **`roll_die.py`**: A Python script, likely used as an exercise to practice executing scripts from the command line, managing permissions, or redirecting output.
- **`test.txt`**: A generic text file, possibly used for practicing file manipulation commands such as `cat`, `grep`, `sed`, or `awk`.
- **`Screenshot 2026-02-25 at 1.34.05 PM.png`**: A screenshot, which might illustrate a specific terminal command output, file system structure, or a successful execution of a script.

This folder demonstrates foundational skills in operating within a command-line interface, which is crucial for development and system administration.